这是一台 AI 驱动的云端个人电脑。

在这台电脑中，内置了一个通用的 AI Agent。它能够理解你的意图，通过指令操作计算机，完成各种任务。
来看一个例子——处理一份 Excel 表格。

[演示 Excel 操作，上传openrouter消耗记录csv,然后发送：分析openrouter消耗记录中gpt-5.4的每条消息的平均使用成本]

[Analyze the average cost per message in GPT-5.4 from the OpenRouter consumption log]

好，完成的不错。那如果我有另一个需求，比如，一个简单的需求，记笔记呢？

[在输入框中输入：将5.4的平均消息成本记录到笔记,但是不发送]

[Record the average message cost of 5.4 in a note]

但是，等下，您知道它当然可以做到，不过，在只有聊天记录的交互里，这并不直观，您记录的内容会淹没在滚动的对话记录里，如果您记录了很多笔记，您无法很好整理和归类，常用的笔记信息无法置顶，想要排版，更是无从谈起。

所以，不能仅仅只是对话。

[双击缩小聊天窗口，桌面和其他应用出现]

看——它实际上不只是一个对话机器人。实际上，它是一个 AI 驱动的操作系统。这里有各种各样的应用，而这些应用，都是您和AI携手共同打造的。

我们预置了一些基础应用，来看一下。

就像传统操作系统有任务管理器一样，AIOS 也有任务管理器。不同的是，这里管理的不是 CPU 任务，而是 Agent 任务——每当应用需要完成一个智能任务，它就会向系统下发一个 Agent 任务，由底层引擎统一执行。

来看 Notebook。新建一篇笔记，输入内容，点击优化——它会调用系统统一的 Agent 引擎，完成这个任务。
[演示优化功能]
看，完成了。

再来看一个更复杂的例子——Crypto Bot。你可以把交易所的 API Key 提供给它，设定你的目标和运行周期。有了 Agent 引擎，你不需要在应用中编写复杂的交易逻辑——它会自动查询行情，寻找机会，并自动决策。
[演示 Crypto Bot]
当然，这只是功能演示，请不要在实盘中轻易尝试，以免造成财产损失。

现在，让我们创建一个新的应用。
你不需要去应用商店寻找，不需要委托开发者——只需要说出你的想法。
让我们来试试，创建一个 [xxx 应用]。
[演示生成过程，刷新，应用出现]
看，来了。
[体验新生成的应用]

这就是 AIOS。
它不只是对话，你可以通过图形界面应用，以更可视化的方式与 AI 协作。
我们相信，对话不会取代图形界面——两者相辅相成，缺一不可。而 AI 时代，应该有 AI 时代的操作系统。

立即访问 wandesk.ai，体验 AI 时代的操作系统。

