# CLAUDE.md

Read `AGENTS.md` first. It is the source of truth for AIOS architecture, source/workspace boundaries, Wandesk workspace behavior, language baking, and modification rules.

If this file is loaded from a Wandesk `workspace` root, treat that directory as the current session's real working directory unless the user explicitly asks to sync from the source repo.
